Django>=5.0,<6.0
djangorestframework>=3.15
django-cors-headers>=4.4
psycopg[binary]>=3.2
python-dotenv>=1.0
requests>=2.32
certifi>=2024.2
beautifulsoup4>=4.12
lxml>=5.2
markitdown[pdf]>=0.0.1a3

# Auth + admin
django-allauth[socialaccount]>=65.0,<66
dj-rest-auth>=7.0,<8
djangorestframework-simplejwt>=5.3,<6
django-phonenumber-field[phonenumbers]>=8.0,<9
whitenoise>=6.7,<7
PyJWT>=2.9
cryptography>=42

# SMS — Twilio. Only imported when TWILIO_ACCOUNT_SID env is set
# (lazy import inside accounts/otp.py::_send_via_twilio); falls back
# to the Django email backend otherwise. Keeping the dep in the
# image so the swap is one env-var change.
twilio>=9.0,<10
